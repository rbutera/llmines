# Tasks: heat-progression-and-tone-sfx

Code-first waves. Each wave ends on the FULL gate set:
`pnpm test` (vitest) + `pnpm typecheck` + `pnpm lint` + `pnpm build` +
`pnpm test:e2e:production-start` + `node scripts/repro-autoplay.mjs <baseURL>`.
The manifest `key` field and any asset step land LAST. No autonomous timeline; no asset re-cut of
the tier audio.

## 1. Heat meter + decay (engine core)

- [x] 1.1 Add the named heat constants to `engine.ts`: `HEAT_GAIN_BASE = 0.06`, `HEAT_GAIN_SQUARE = 0.025`, `HEAT_GAIN_COMBO = 0.02`, `HEAT_DECAY_PER_EMPTY_PASS = 0.08`. Do NOT add an `ADVANCE_HEAT` constant — the advance is gated on the top tier being audible + held, not a heat threshold (design D4). Document each against the loop-boundary cadence (design D1-D4); note that decay (0.08) is below a typical clear gain (0.11) so alternation does not thrash a layer.
- [x] 1.2 Add engine state: `private heat = 0` and `private clearedSinceBoundary = false`. Add a `clamp01` helper.
- [x] 1.3 Rewrite `onScore` (rename to `onClear(squares, combo)` or keep `onScore(weight)` feeding heat): a clear adds `HEAT_GAIN_BASE + HEAT_GAIN_SQUARE*squares + HEAT_GAIN_COMBO*combo`, clamped 0..1; a chain adds `HEAT_GAIN_BASE + HEAT_GAIN_SQUARE*min(8,size)`. Ignore non-finite contributions; re-clamp `heat` to 0..1. Set `clearedSinceBoundary = true` on any clear/chain.
- [x] 1.4 Wire `play()` (the `lineClear` / `chain` branches) to feed heat via the new path using the real `(squares, combo)` from the event (combo = streak offset already on the event).
- [x] 1.5 In `onLoopBoundary`, at the start, apply decay: if `!clearedSinceBoundary` then `heat = clamp01(heat - HEAT_DECAY_PER_EMPTY_PASS)`; then reset `clearedSinceBoundary = false` for the next pass.
- [x] 1.6 Add `heat` to `getAudioState()`; keep `intensity` as a DEPRECATED alias (= `heat`). REMOVE `segmentScore` from the probe ENTIRELY — do NOT keep a deprecated numeric `segmentScore` alias (a numeric alias would silently back the old contract's zombie assertions). Migrate every consumer to `heat` (see 5.x for the e2e `AudioState` interface + assertions).
- [x] 1.7 Update the dev hooks: `__injectClears(n)` banks `n` typical clears' worth of heat (UP path); `__stepBoundary()` runs the next boundary and decays when no clear was injected since the prior boundary. Add a DISTINCT `__decayPasses(n)` (NEW) that steps `n` loop boundaries each GUARANTEEING `clearedSinceBoundary = false` (forces the no-clear flag false before each boundary so every step decays) — the deterministic DOWN-path driver, kept SEPARATE from the `__injectClears` + `__stepBoundary` UP path so the two are never conflated.
- [x] 1.8 Unit tests (vitest): heat gain scaling by squares/combo, saturation at 1.0, non-finite ignored, decay on a clear-less boundary, no-decay after a clear, compounding decay over consecutive empty passes, and the probe reports `heat`.
- [x] 1.9 Run the full gate set; fix to green.

## 2. Heat -> tier mapping (up and down) + carry-across

- [x] 2.1 Replace `evaluateTier`'s `revealed = floor(segmentScore/STEP)` logic with `desiredTier = round(heat * maxTier(seg))`, floored at `tierFloorFor(seg)`, ceilinged at `maxTier(seg)`, demoted via `nearestAvailableAtOrBelow`.
- [x] 2.2 Change the boundary tier move to ONE STEP toward `desiredTier` in EITHER direction (allow DOWN). Remove the sticky-up-only `Math.max(..., this.tier, ...)` clamp so the tier can shed. Keep the one-tier-to-one-tier crossfade (no-hiss invariant intact). Confirm `swapTier` handles a downward crossfade.
- [x] 2.3 Rewrite `enterSegment` to set `startTier = clamp(round(heat * maxTier(seg)), tierFloorFor(seg), maxTier(seg))` then `nearestAvailableAtOrBelow`. REMOVE the `segmentScore = 0` reset and the `entryFloor` `top-1` cap. Retire `entryFloor` as the carry mechanism (heat is the single carry state). Keep resetting `topHeldSinceBoundary` on entry.
- [x] 2.4 Remove the now-dead `segmentScore`, `TIER_REVEAL_STEP`, `ADVANCE_THRESHOLD`, `entryFloor` field and their references (keep `TIER_ENTRY_FLOOR` / `MIN_AUDIBLE_LAYERS` / `tierFloorFor` for the floor). Update `reconcileActiveGain` to use the heat-derived target.
- [x] 2.5 Unit tests: rising heat reveals one step per boundary, falling heat sheds one step per boundary, the floor is never breached at heat 0, and the no-hiss bound (≤2 active bed players across a crossfade) holds. Add the CARRY-ACROSS test: at top tier with heat ~1.0, advance, assert post-entry tier == top (no cut); with dropped heat, assert post-entry tier < previous top.
- [x] 2.6 Run the full gate set; fix to green.

## 3. Heat-gated advance + loop retention

- [x] 3.1 Rewrite `shouldAdvance(seg, tierBefore)` to the design D4 conjunction (NO bare-heat path): `!transitionInFlight` AND the no-advance-on-reveal-boundary guard (`!(tierBefore < top && this.tier >= top)`) AND `this.tier >= maxTier(seg)` (top tier audible — ALL layers built) AND `topHeldSinceBoundary` (top held one full loop). Do NOT add any `heat >= ADVANCE_HEAT` clause. Drop the old `segmentScore >= ADVANCE_THRESHOLD` and the three-gate mandatory block (its intent is now this single gate). Rationale to keep in the comment: a heat threshold below 1.0 could fire before the top tier is audible (song2 top reveals at heat ≥ 0.875) and skip unheard vocals.
- [x] 3.2 Confirm `advanceSegment` is unchanged in mechanism (forward-only, one-step, in-flight-locked, token-guarded, carries via heat now not `entryFloor`); confirm the TERMINAL/end-of-song `onSongComplete` path is intact.
- [x] 3.3 DELETE/REWRITE the stale integration-test describe blocks in `src/game/audio/procedural/engine.integration.test.ts` that assert the OLD model as the contract — do NOT vaguely "update" them, rewrite each to the heat model: the `clear-gated engine: loop-in-place, sticky reveal, gated advance` block (419), `mandatory advance on full reveal (vocals in → must move on)` (1215), `clear-gated progression (audio-truth D3) — the 6 contract cases` (1369), and any assertion of "carried floor caps at top-1" / "sticky never sheds" / a `segmentScore` read. Rewrite them to: heat gain/decay, layers up AND down, carry-across (top tier carried, no `top-1` cap), top-built-and-held advance (no `ADVANCE_HEAT`), and loop-in-place below the gate. Keep the structural blocks that are model-agnostic (SFX voice pool, switchTrack, N-tier generalization, advance-into-unloaded re-gain, resetForNewGame, min-2-layers, per-segment SFX) but fix any `segmentScore` reference in them to `heat`.
- [x] 3.4 Unit + e2e (audio-structure.spec): topped-out + held-one-loop advances exactly one segment; a segment below its top tier with high heat does NOT advance (no bare-heat fast-forward); a 4-tier (song1) segment advances only after the audible tier == 3 and held a loop; a 5-tier (song2) segment driven through the 0.85–0.87 heat band does NOT advance while audible tier ≤ 3 and advances only after audible tier == 4 held a loop; a 40-clear burst advances at most one segment per `__stepBoundary` (no fast-forward); the index never decreases; no advance on the reveal boundary; advancing off the last segment fires `onSongComplete`.
- [x] 3.5 Run the full gate set; fix to green.

## 4. Tone SFX engine + match event + selector

- [x] 4.1 Add `type SfxMode = "tone" | "sample"` and `private sfxMode: SfxMode = "tone"` with `setSfxMode(mode)`; expose `sfxMode` on the probe.
- [x] 4.2 Add a LAZILY-constructed Tone synth (PolySynth/Synth) built inside `unlock()` / on first tone use, routed through `this.master`, with a short subtle envelope. Never construct at module-eval (autoplay rule). Degrade to silence on any failure.
- [x] 4.3 Build the in-key note set per song from a resolved `key` (root + scale degrees), with the default `{ root: "A", scale: "minor" }` when absent. Implement the design D6 tone palette (match = high consonant degree with a per-square brightness nudge; rotate = root; softDrop = degree 2; lock = root one octave down scaled by cause; **move + sweep clear + chain = silent**). Do NOT add a chain arpeggio/figure to the tone palette.
- [x] 4.4 Add the `match` event to `AudioEvent`: `{ type: "match"; squares: number }`. In `play()`, route `match` to the tone ding (tone mode) or the `stage` sample (sample mode, using `stageVelocityForSquares(squares)` for parity); keep BOTH the sweep `lineClear` AND the `chain` SILENT in tone mode (heat still fed) and keep the recorded routing (incl. chain's `stage`+`drop`) in sample mode.
- [x] 4.5 Update `sfxRouting.ts`: add `match` routing under the selector; in tone mode `lineClear`/`chain`/`move` are silent and `match`/`rotate`/`softDrop`/`lock` route to tones; keep the recorded `stage`/`drop`/`rotate`/`softdrop` map (incl. chain → `stage`+`drop`) for sample mode. Keep `routeEvent` pure/unit-testable.
- [x] 4.6 Derive the `match` event in `events.ts` decoupled from `lastLock.id`: emit `match` when the render-only `markedSquares` count RISES vs the previous frame, on ANY frame; carry `squares = positive delta`. A DECREASE emits nothing (clear silent). Do NOT gate on `lastLock.id` — it is bumped only by `lockPiece` (a piece settle), NOT by a post-clear gravity cascade, so a cascade-formed square would be missed. Add the controller render-only `markedSquares: number` projection on `RenderState` = `computeMarked(grid).distinctSquares` (already returned by `computeMarked`, already on `PublicState`) — additive, render-only, zero determinism impact. Update the events.ts `TelemetryCarrier`/snapshot to read `markedSquares` (no longer needs the lock-id gate for `match`).
- [x] 4.7 Unit tests: tone notes are members of the song scale; the synth is not constructed at import (assert no AudioContext pre-gesture); default mode is `tone`; sample mode uses the recorded path (match reuses `stageVelocityForSquares`); runtime mode switch; `match` emitted on a `markedSquares` RISE (incl. a cascade-formed square with NO lock-id advance) and NOT on the erasing sweep (count decrease); bigger rise -> higher velocity; rotate/softDrop/lock tone, move silent, sweep clear silent, chain silent (tone mode).
- [x] 4.8 Run the full gate set; fix to green.

## 5. e2e carry-across observable + strict-autoplay

- [x] 5.1 Update the `e2e/audio-structure.spec.ts` `AudioState` interface and all assertions: replace `segmentScore` with `heat` (the probe no longer exposes `segmentScore` — 1.6). Extend the spec: drive heat to the top tier via `__injectClears` + `__stepBoundary` to advance, and ASSERT the post-transition `tier` >= the pre-transition `tier` (the no-vocal-cut carry-across observable). Add a DOWN case using the DISTINCT `__decayPasses(n)` hook (NOT `__stepBoundary`): decay until a layer sheds, assert `tier` decreased. Add a no-bare-heat assertion: a 5-tier segment held in the 0.85–0.87 heat band does NOT advance while audible tier ≤ 3.
- [x] 5.2 Confirm `scripts/repro-autoplay.mjs` still measures real RMS > floor with the tone SFX path active (tones are subtle; ensure the harness's RMS floor still distinguishes real output). Adjust the harness only if the subtle tones legitimately sit under the existing floor; do not weaken the no-output detection.
- [x] 5.3 Run the full gate set incl. `pnpm test:e2e:production-start` and `node scripts/repro-autoplay.mjs http://localhost:3201`; fix to green.

## 6. Manifest key + default + README (last)

- [x] 6.1 Add the optional `key?: { root: string; scale: string }` field to the `ManifestSong` type and the manifest contract; apply the engine default `{ root: "A", scale: "minor" }` when absent (the existing committed `manifest.json` works unchanged with no `key`).
- [x] 6.2 (Asset/data step — MUST land BEFORE the tone ear-check in 6.4) Author `key` for `song1` and `song2` in `public/audio/manifest.json` (and the `transcode-and-manifest.py` emit if regenerating) — hand-keyed per-song values. The first ear-check is otherwise on the DEFAULT `A minor` placeholder, so authoring the real per-song keys first means Rai judges the tones in the actual song keys. No tier audio re-cut.
- [x] 6.3 Rewrite the README §2 audio-model section: heat-driven progression (layers follow heat UP and DOWN, carry across segments, forward-only advance gated on the TOP TIER being audible AND held a full loop — "opens at the end once all the layers are built up", with NO bare-heat threshold so unheard material is never skipped, loop retained), and the tone-SFX model (sweep clear = silent; chain = silent; match = in-key ding; rotate/drop = subtle in-key tones; recorded sample path kept behind the `"tone" | "sample"` selector; manifest `key`). Note the heat constants (no `ADVANCE_HEAT`).
- [x] 6.4 Final full gate set green; ready for Rai's ear-check (on the authored per-song keys from 6.2) before `pnpm cf:deploy`.

## 7. Validate

- [x] 7.1 `openspec validate heat-progression-and-tone-sfx --strict` passes.
